#convert Text export from VegLog MS Access Database
# library(vegnasis)
# veg.raw <- vegnasis::nasis.veg
#
# obss <- obs |> mutate(date = paste0(Year,"-", Mon,"-",  Day))
# obss <- obsspp |> left_join(obs[obs$Day paste0('Year',"-", 'Mon',"-",  'Day'), Observation_ID, User_Plot_ID)
#
# obss <- obs |> mutate(date = as.Date(ISOdate(year = Year,
#         month = Mon,
#         day = Day)))
